<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.openstudy.notes.mapper.NotePublicSectionMapper">

    <resultMap type="NotePublicSection" id="NotePublicSectionResult">
        <result property="id"    column="id"    />
        <result property="name"    column="name"    />
        <result property="sortOrder"    column="sort_order"    />
        <result property="createTime"    column="create_time"    />
    </resultMap>

    <select id="selectAll" resultMap="NotePublicSectionResult">
        SELECT * FROM note_public_section ORDER BY sort_order
    </select>

</mapper>
